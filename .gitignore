# Ignore sensitive or machine-specific files
*.env
*.key
*.pem

# Symlinks pointing outside the repo (would be broken for others)
# - patent-search → ~/.claude-config-private/skills/patent-search (private repo)
# - find-skills   → external skill folder
skills/patent-search
skills/find-skills
